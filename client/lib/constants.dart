const String LOCAL_ASSET_DIR = 'assets'; // Should match `pubspec.yaml` entry.

// const String API_URL = 'http://jbourde2.w3.uvm.edu/game';
const String API_URL = 'http://127.0.0.1:5000';

enum JailMethod {
  doubles,
  money,
  card,
}
